# @package _global_
hydra:
  sweep:
    dir: output/pdeparamsweep-full/gamma_${solver.gamma}-kappa_${solver.kappa}/ic_${train.ic}/
    subdir: seed_${train.random_seed}
  sweeper:
    params:
      train.random_seed: 0,1,2

  launcher:
    n_jobs: 1